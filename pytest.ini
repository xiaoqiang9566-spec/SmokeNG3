[pytest]
addopts = -ra
pythonpath = src
testpaths = tests
markers =
    device: requires a real watch and local SDSApplicationServer
    yaml: YAML scenario cases
    smoke: smoke test cases
    regression: regression test cases
    stability: long-running device checks
